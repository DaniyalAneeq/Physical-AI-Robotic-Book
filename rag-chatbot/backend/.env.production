# Production Environment Configuration (Unified Backend)
# This file contains settings for production deployment (HTTPS)

# OpenAI Configuration
OPENAI_API_KEY=REPLACE-WITH-PRODUCTION-API-KEY

# Qdrant Configuration
QDRANT_URL=https://your-qdrant-cloud-url.qdrant.io
QDRANT_API_KEY=REPLACE-WITH-PRODUCTION-QDRANT-KEY
QDRANT_COLLECTION_NAME=content-aidd-book

# Database Configuration
DATABASE_URL=REPLACE-WITH-PRODUCTION-DATABASE-URL

# Application Configuration
ENVIRONMENT=production
LOG_LEVEL=INFO

# CORS Configuration (Production origins only)
CORS_ORIGINS=https://yourusername.github.io,https://your-backend-domain.com

# Embedding Configuration
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536

# Content Paths
TEXTBOOK_DOCS_PATH=../../AIdd-book/docs

# Authentication Configuration
SESSION_SECRET=REPLACE-WITH-SECURE-RANDOM-SECRET

# Cookie Configuration (Production - HTTPS)
# CRITICAL: Use SameSite=None with Secure=True for cross-origin (HTTPS only)
SECURE_COOKIES=true
SAME_SITE_COOKIES=none

# OAuth Configuration (Google)
OAUTH_GOOGLE_CLIENT_ID=your-production-google-client-id
OAUTH_GOOGLE_CLIENT_SECRET=your-production-google-client-secret
OAUTH_GOOGLE_REDIRECT_URI=https://your-backend-domain.com/auth/oauth/google/callback

# OAuth Configuration (GitHub)
OAUTH_GITHUB_CLIENT_ID=your-production-github-client-id
OAUTH_GITHUB_CLIENT_SECRET=your-production-github-client-secret
OAUTH_GITHUB_REDIRECT_URI=https://your-backend-domain.com/auth/oauth/github/callback

# Frontend URL (Production GitHub Pages)
FRONTEND_URL=https://yourusername.github.io/Physical-AI-Robotic-Book
