# Backend dependencies for RAG Chatbot
# Pinned versions per plan.md

# OpenAI ChatKit SDK (official)
openai-chatkit>=1.4.0
openai>=1.50.0
openai-agents>=0.0.15

# Web Framework
fastapi>=0.115.0
uvicorn>=0.30.0

# Vector Database
qdrant-client>=1.10.0

# PostgreSQL (Neon)
psycopg[binary,pool]>=3.2.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.8.0
pydantic-settings>=2.3.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
